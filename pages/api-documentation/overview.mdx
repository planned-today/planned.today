import Badge from "../../components/badge";

## Response Data Structure

The response data follows the following structure throughout the API:

### Success Response

```typescript
{
    "status": 200;
    "data": object;
}
```

### Error Response

```typescript
{
    "status": 500| 400 | 401;
    "message": string;
    "data": object | undefined;
}
```

---

## API Endpoints

The API currently supports the following endpoints:

### Utility

#### <Badge href="/api-documentation/endpoints/api">`/api`</Badge>

> Get information about the API and list its endpoints

#### <Badge href="/api-documentation/endpoints/ping">`/api/ping`</Badge>

> Check if the server is running

### Data

#### <Badge href="/api-documentation/endpoints/api">`/api/schedule/:id`</Badge>

> Get personalised schedule for a specific user
