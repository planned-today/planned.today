import Badge from "../../../components/badge";
import Method from "../../../components/method";

# <Badge href="/api-documentation/endpoints/ping">`/api/ping`</Badge>

<br />
<Method
	href="#get"
	method="GET"
/>

This endpoint always responds with the following JSON to test that the server is up and running.

```json
{
	"status": 200,
	"data": {
		"message": "pong!"
	}
}
```
